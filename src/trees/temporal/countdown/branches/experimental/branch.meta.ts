import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: Experimental (on Countdown)
 *
 * Looks where the mechanism is the point — the clock behaves like an object
 * rather than a label. Same contract, louder answer.
 */
export const meta: BranchMeta = {
  label: "Experimental",
  description: "The clock as an object, not a label. Same contract, louder answer.",
};

export default meta;
