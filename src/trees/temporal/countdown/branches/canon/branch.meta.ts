import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: Canon (on Countdown)
 *
 * The digits stay readable. Motion marks urgency, it never obscures the number
 * — a countdown you have to wait for the animation to finish before you can
 * read is a countdown that failed at its one job. Every leaf here holds still
 * enough to be screenshotted.
 */
export const meta: BranchMeta = {
  label: "Canon",
  description: "The number stays readable. Motion marks urgency; it never hides a digit.",
};

export default meta;
