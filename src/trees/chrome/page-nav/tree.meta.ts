import type { TreeMeta } from "@/lib/forest";

export const meta: TreeMeta = {
  label: "Page Nav",
  description:
    "One nav contract covering laughingwhales.com's creator page tab bar and a brand · links · CTA marketing bar. Items carry a pre-resolved active state, overflow chevrons are data, and `adaptPageTabBar()` lifts the creator bar's props in one call.",
  tags: ["nav", "chrome", "portable", "condensing"],
};

export default meta;
