import type { TreeMeta } from "@/lib/forest";

export const meta: TreeMeta = {
  label: "Pane Dock",
  description:
    "The tree that answers one question: where do the closed panes go? A workspace opens the panes its job needs and has to put the rest somewhere — and the obvious answer, a strip on the edge for each one, is individually cheap and collectively fatal. Its leaves are four different doors onto the same two arrays, so the chrome budget of a surface can be changed without the surface learning anything.",
  tags: ["workspace", "chrome", "layout", "panes", "disclosure", "posture"],
};

export default meta;
