import type { TreeMeta } from "@/lib/forest";

/**
 * Tree: Clip Picker (finding)
 */
export const meta: TreeMeta = {
  label: "Clip Picker",
  description:
    "The tree that answers 'what do I send this streamer?'. Two ways in on one surface — shelves the streamer curated, and transcript search for the viewer who has the words — with the send living on the card so nobody is ever asked to look in two places at once.",
  tags: ["send", "search", "transcript", "curation", "discovery", "viewer"],
};

export default meta;
