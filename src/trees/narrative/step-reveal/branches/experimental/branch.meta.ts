import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: Experimental (on Step Reveal)
 *
 * One step at a time, display type, big gestures. Trades scannability for
 * impact — the trade is the point.
 */
export const meta: BranchMeta = {
  label: "Experimental",
  description: "One step at a time, display type, big gestures. Impact over scannability.",
};

export default meta;
