import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: Canon (on Step Reveal)
 *
 * Every step stays readable at once. These are the looks you ship when the
 * content matters more than the transition.
 */
export const meta: BranchMeta = {
  label: "Canon",
  description: "All steps readable at once — the motion marks position, it never hides content.",
};

export default meta;
