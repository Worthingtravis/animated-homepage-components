import type { TreeMeta } from "@/lib/forest";

export const meta: TreeMeta = {
  label: "Step Reveal",
  description:
    "A 'how it works' sequence that advances on its own and can be steered. Autoplay, click-to-jump, and reduced motion all resolve to one contract.",
  tags: ["how-it-works", "sequence", "steerable"],
};

export default meta;
