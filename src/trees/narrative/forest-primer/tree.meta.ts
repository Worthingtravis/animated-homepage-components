import type { TreeMeta } from "@/lib/forest";

export const meta: TreeMeta = {
  label: "Forest Primer",
  description:
    "A scroll-driven primer that explains the forest's own architecture — six chapters, every " +
    "number in them derived from FOREST rather than written down.",
  tags: ["scroll", "explainer", "diagram", "derived"],
};

export default meta;
