import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: Canon (on Forest Primer)
 *
 * A branch is an aesthetic direction, not a data variation — every leaf here
 * still consumes ForestPrimerVM unchanged. If a "variant" needs different data,
 * it is a new TREE, not a new branch.
 */
export const meta: BranchMeta = {
  label: "Canon",
  description:
    "Editorial and quiet — numbered chapters, generous rules between them, and every diagram " +
    "drawn in plain CSS and SVG rather than illustrated.",
};

export default meta;
