import type { TreeMeta } from "@/lib/forest";

export const meta: TreeMeta = {
  label: "Product Card",
  description:
    "One purchasable thing: a picture, what it is, what it costs, and the two presses a card is allowed to own. The money is the point — the discount badge, the struck compare-at price and the saving line are three views of one calculation done in the container, so no two variants can ever disagree about the price.",
  tags: ["commerce", "product", "price", "cart", "card"],
};

export default meta;
