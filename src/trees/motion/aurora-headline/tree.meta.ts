import type { TreeMeta } from "@/lib/forest";

export const meta: TreeMeta = {
  label: "Aurora Headline",
  description:
    "A hero headline whose supporting chips drift on a shared clock. The reference tree for this repo.",
  tags: ["hero", "headline", "clock"],
};

export default meta;
