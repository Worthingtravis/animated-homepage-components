import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: Experimental (on Aurora Headline)
 *
 * Loud, opinionated looks. Same contract, no restraint. Leaves here are allowed
 * to be too much — that is the point of keeping them off `canon`.
 */
export const meta: BranchMeta = {
  label: "Experimental",
  description: "Loud, opinionated looks. Same contract, none of the restraint.",
};

export default meta;
