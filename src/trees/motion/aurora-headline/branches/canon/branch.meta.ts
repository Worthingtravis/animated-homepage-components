import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: Canon (on Aurora Headline)
 *
 * Restrained, production-safe looks. A canon leaf should be droppable into a
 * marketing page without a design review.
 */
export const meta: BranchMeta = {
  label: "Canon",
  description: "Restrained, production-safe looks — quiet type, low-amplitude motion.",
};

export default meta;
