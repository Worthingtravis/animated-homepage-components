import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: Canon (on Layered Poster)
 *
 * A branch is an aesthetic direction, not a data variation — every leaf here
 * still consumes LayeredPosterVM unchanged. If a "variant" needs different data,
 * it is a new TREE, not a new branch.
 */
export const meta: BranchMeta = {
  label: "Canon",
  description:
    "The plates straight — a square sheet, nothing cropped, nothing rearranged. The composition lives in the artwork.",
};

export default meta;
