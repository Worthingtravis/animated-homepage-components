import type { TreeMeta } from "@/lib/forest";

export const meta: TreeMeta = {
  label: "Layered Poster",
  description:
    "A stack of transparent art plates that arrive out of their own depth under one settling camera — the extract-vm form of a layered key-art poster.",
  tags: ["poster", "parallax", "depth", "entrance", "key-art"],
};

export default meta;
