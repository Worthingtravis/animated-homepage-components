import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: Broadcast (on Channel Hero)
 *
 * Looks that borrow from on-stream furniture — tickers, lower-thirds, tally
 * lights. Loud on purpose. These belong on a page whose only job is the channel.
 */
export const meta: BranchMeta = {
  label: "Broadcast",
  description: "On-stream furniture: tickers, lower-thirds, a tally light. Loud on purpose.",
};

export default meta;
