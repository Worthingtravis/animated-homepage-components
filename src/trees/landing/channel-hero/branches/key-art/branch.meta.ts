import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: Key Art (on Channel Hero)
 *
 * The hero as a printed one-sheet. Box art, tour posters, game covers: a hard
 * grid of panels, a wordmark cut straight through the middle of it, and a
 * billing block along the bottom. Where Canon settles down so the page can
 * speak and Broadcast borrows on-stream furniture, Key Art borrows from print —
 * the entrance is a camera move onto a poster that was already finished, not a
 * UI assembling itself.
 */
export const meta: BranchMeta = {
  label: "Key Art",
  description:
    "The hero as a printed one-sheet: a panel mosaic, a wordmark cut through the middle, a billing block underneath.",
};

export default meta;
