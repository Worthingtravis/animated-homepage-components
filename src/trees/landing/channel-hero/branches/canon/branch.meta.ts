import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: Canon (on Channel Hero)
 *
 * Restrained looks that survive being embedded in a page that has other things
 * to say. Motion marks arrival and then gets out of the way.
 */
export const meta: BranchMeta = {
  label: "Canon",
  description: "Restrained heroes. The entrance settles and then the page belongs to the content.",
};

export default meta;
