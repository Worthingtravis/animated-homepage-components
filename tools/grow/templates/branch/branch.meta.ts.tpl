import type { BranchMeta } from "@/lib/forest";

/**
 * Branch: __BRANCH_TITLE__ (on __TREE_TITLE__)
 *
 * A branch is an aesthetic direction, not a data variation — every leaf here
 * still consumes __VM_TYPE__ unchanged. If a "variant" needs different data,
 * it is a new TREE, not a new branch.
 */
export const meta: BranchMeta = {
  label: "__BRANCH_TITLE__",
  description: "TODO: what visual direction do the leaves on this branch share?",
};

export default meta;
